// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
	site: "REPLACE WITH YOUR GITHUB PAGES BASE URL (ie. https://<username>.github.io)",
	base: "REPLACE WITH YOUR GITHUB REPOSITORY NAME (ie. /<repository-name>)",
});
